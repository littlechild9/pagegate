# PageGate example configuration
# Copy this file to config.yaml and fill in your own values.

admin_token: "replace-with-your-admin-token"

# User registration: "open" allows anyone to register, "closed" disables registration
registration:
  mode: "open"

# Product-layer vs instance-layer branding
branding:
  product_name: "PageGate"
  instance_name: "Xuan & Friends' PageGate"

server:
  host: "0.0.0.0"
  port: 8888
  base_url: "http://115.190.148.77:8888"
  session_secret: "replace-with-your-session-secret"

dingtalk:
  app_key: ""
  app_secret: ""

wechat:
  app_id: ""
  app_secret: ""

openclaw:
  webhook_url: ""
  webhook_token: ""
